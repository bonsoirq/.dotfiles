# Dotfiles

## Setup

Run `setup.sh`

## Package manager dumps

### Homebrew

  Running dump:

  `brew bundle dump --describe --force`

  Restoring dump:

  `brew bundle`

### Pacman or Yay

  Running dump:

  `yay -Qe > yayfile`
